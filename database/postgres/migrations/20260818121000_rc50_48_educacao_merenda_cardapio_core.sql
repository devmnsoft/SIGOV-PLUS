begin;
create schema if not exists sigov;
create table if not exists sigov.educacao_merenda_produto (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_produto add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_produto_tenant_status on sigov.educacao_merenda_produto(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_fornecedor (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_fornecedor add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_fornecedor_tenant_status on sigov.educacao_merenda_fornecedor(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_estoque (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_estoque add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_estoque_tenant_status on sigov.educacao_merenda_estoque(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_movimento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_movimento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_movimento_tenant_status on sigov.educacao_merenda_movimento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_cardapio (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_cardapio add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_cardapio_tenant_status on sigov.educacao_merenda_cardapio(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_cardapio_item (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_cardapio_item add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_cardapio_item_tenant_status on sigov.educacao_merenda_cardapio_item(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_distribuicao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_distribuicao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_distribuicao_tenant_status on sigov.educacao_merenda_distribuicao(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_distribuicao_item (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_distribuicao_item add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_distribuicao_item_tenant_status on sigov.educacao_merenda_distribuicao_item(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_consumo (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_consumo add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_consumo_tenant_status on sigov.educacao_merenda_consumo(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_requisicao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_requisicao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_requisicao_tenant_status on sigov.educacao_merenda_requisicao(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_requisicao_item (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_requisicao_item add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_requisicao_item_tenant_status on sigov.educacao_merenda_requisicao_item(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.educacao_merenda_evento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, ano_letivo integer, escola_id bigint, aluno_id bigint, turma_id bigint, servidor_id bigint, referencia_id bigint, rota_id bigint, veiculo_id bigint, motorista_id bigint, produto_id bigint, acervo_id bigint, exemplar_id bigint, centro_custo_id bigint, codigo varchar(120), numero bigint, nome varchar(240), tipo varchar(80), categoria varchar(120), turno varchar(40), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, unidade varchar(40), quantidade numeric(18,4), saldo numeric(18,4), saldo_minimo numeric(18,4), valor numeric(18,2), competencia date, data_inicio date, data_fim date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.educacao_merenda_evento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_educacao_merenda_evento_tenant_status on sigov.educacao_merenda_evento(tenant_id,status) where is_deleted=false and ativo=true;
commit;
