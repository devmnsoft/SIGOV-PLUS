begin;
create schema if not exists sigov;
-- RC50.47: núcleo preparatório, multi-tenant, auditável e sem integração externa declarada.
create table if not exists sigov.tributario_fiscalizacao_projeto (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_projeto add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_projeto_tenant_status on sigov.tributario_fiscalizacao_projeto(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_grupo (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_grupo add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_grupo_tenant_status on sigov.tributario_fiscalizacao_grupo(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_ordem (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_ordem add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_ordem_tenant_status on sigov.tributario_fiscalizacao_ordem(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_auditor (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_auditor add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_auditor_tenant_status on sigov.tributario_fiscalizacao_auditor(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_cronograma (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_cronograma add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_cronograma_tenant_status on sigov.tributario_fiscalizacao_cronograma(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_diligencia (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_diligencia add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_diligencia_tenant_status on sigov.tributario_fiscalizacao_diligencia(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_termo_inicio (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_termo_inicio add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_termo_inicio_tenant_status on sigov.tributario_fiscalizacao_termo_inicio(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_notificacao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_notificacao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_notificacao_tenant_status on sigov.tributario_fiscalizacao_notificacao(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_auto_infracao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_auto_infracao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_auto_infracao_tenant_status on sigov.tributario_fiscalizacao_auto_infracao(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_auto_item (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_auto_item add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_auto_item_tenant_status on sigov.tributario_fiscalizacao_auto_item(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_defesa (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_defesa add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_defesa_tenant_status on sigov.tributario_fiscalizacao_defesa(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_julgamento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_julgamento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_julgamento_tenant_status on sigov.tributario_fiscalizacao_julgamento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_documento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_documento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_documento_tenant_status on sigov.tributario_fiscalizacao_documento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_fiscalizacao_evento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_fiscalizacao_evento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_fiscalizacao_evento_tenant_status on sigov.tributario_fiscalizacao_evento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_simples_divergencia (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_simples_divergencia add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_simples_divergencia_tenant_status on sigov.tributario_simples_divergencia(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.tributario_iss_apuracao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, contribuinte_id bigint, cadastro_imobiliario_id bigint, cadastro_economico_id bigint, lancamento_id bigint, guia_id bigint, referencia_id bigint, codigo varchar(120), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'RASCUNHO', descricao text, justificativa text, quantidade numeric(18,4), valor numeric(18,2), competencia date, prazo_at timestamptz, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint);
alter table sigov.tributario_iss_apuracao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(40) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_tributario_iss_apuracao_tenant_status on sigov.tributario_iss_apuracao(tenant_id,status) where is_deleted=false and ativo=true;
commit;
