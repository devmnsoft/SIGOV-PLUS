-- SIGOV+ RC50.42 - Bloco 8. Idempotente, preserva dados e suporta bancos parcialmente migrados.

create schema if not exists sigov;

create or replace function sigov.rc50_42_ensure_common_columns(target regclass) returns void language plpgsql as $fn$ begin
 execute format('alter table %s add column if not exists tenant_id bigint',target); execute format('alter table %s add column if not exists entidade_id bigint',target);
 execute format('alter table %s add column if not exists exercicio_id bigint',target); execute format('alter table %s add column if not exists codigo varchar(100)',target);
 execute format('alter table %s add column if not exists numero bigint',target); execute format('alter table %s add column if not exists ano integer',target);
 execute format('alter table %s add column if not exists tipo varchar(80)',target); execute format('alter table %s add column if not exists status varchar(40)',target);
 execute format('alter table %s add column if not exists titulo varchar(300)',target); execute format('alter table %s add column if not exists descricao text',target);
 execute format('alter table %s add column if not exists referencia_id bigint',target); execute format('alter table %s add column if not exists responsavel_id bigint',target);
 execute format('alter table %s add column if not exists data_referencia timestamptz',target); execute format('alter table %s add column if not exists prazo_at timestamptz',target);
 execute format('alter table %s add column if not exists justificativa text',target); execute format('alter table %s add column if not exists sigiloso boolean',target);
 execute format('alter table %s add column if not exists hash_sha256 varchar(64)',target); execute format('alter table %s add column if not exists codigo_validacao varchar(100)',target);
 execute format('alter table %s add column if not exists dados jsonb',target); execute format('alter table %s add column if not exists auditoria jsonb',target);
 execute format('alter table %s add column if not exists correlation_id uuid',target); execute format('alter table %s add column if not exists ativo boolean',target);
 execute format('alter table %s add column if not exists is_deleted boolean',target); execute format('alter table %s add column if not exists created_at timestamptz',target);
 execute format('alter table %s add column if not exists updated_at timestamptz',target); execute format('alter table %s add column if not exists deleted_at timestamptz',target);
 execute format('alter table %s add column if not exists created_by bigint',target); execute format('alter table %s add column if not exists updated_by bigint',target); execute format('alter table %s add column if not exists deleted_by bigint',target);
 execute format('update %s set status=coalesce(status,''ATIVO''), dados=coalesce(dados,''{}''::jsonb), auditoria=coalesce(auditoria,''{}''::jsonb), correlation_id=coalesce(correlation_id,gen_random_uuid()), ativo=coalesce(ativo,true), is_deleted=coalesce(is_deleted,false), sigiloso=coalesce(sigiloso,false), created_at=coalesce(created_at,now())',target);
 execute format('alter table %s alter column status set default ''ATIVO'', alter column dados set default ''{}''::jsonb, alter column auditoria set default ''{}''::jsonb, alter column correlation_id set default gen_random_uuid(), alter column ativo set default true, alter column is_deleted set default false, alter column sigiloso set default false, alter column created_at set default now()',target);
end $fn$;

create table if not exists sigov.legislativo_parlamentar (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_parlamentar'::regclass);

create index if not exists ix_legislativo_parlamentar_tenant_status on sigov.legislativo_parlamentar(tenant_id, status);

create index if not exists ix_legislativo_parlamentar_tenant_ativo on sigov.legislativo_parlamentar(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_legislatura (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_legislatura'::regclass);

create index if not exists ix_legislativo_legislatura_tenant_status on sigov.legislativo_legislatura(tenant_id, status);

create index if not exists ix_legislativo_legislatura_tenant_ativo on sigov.legislativo_legislatura(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_mesa_diretora (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_mesa_diretora'::regclass);

create index if not exists ix_legislativo_mesa_diretora_tenant_status on sigov.legislativo_mesa_diretora(tenant_id, status);

create index if not exists ix_legislativo_mesa_diretora_tenant_ativo on sigov.legislativo_mesa_diretora(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_comissao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_comissao'::regclass);

create index if not exists ix_legislativo_comissao_tenant_status on sigov.legislativo_comissao(tenant_id, status);

create index if not exists ix_legislativo_comissao_tenant_ativo on sigov.legislativo_comissao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_comissao_membro (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_comissao_membro'::regclass);

create index if not exists ix_legislativo_comissao_membro_tenant_status on sigov.legislativo_comissao_membro(tenant_id, status);

create index if not exists ix_legislativo_comissao_membro_tenant_ativo on sigov.legislativo_comissao_membro(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_proposicao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_proposicao'::regclass);

create index if not exists ix_legislativo_proposicao_tenant_status on sigov.legislativo_proposicao(tenant_id, status);

create index if not exists ix_legislativo_proposicao_tenant_ativo on sigov.legislativo_proposicao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_proposicao_autor (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_proposicao_autor'::regclass);

create index if not exists ix_legislativo_proposicao_autor_tenant_status on sigov.legislativo_proposicao_autor(tenant_id, status);

create index if not exists ix_legislativo_proposicao_autor_tenant_ativo on sigov.legislativo_proposicao_autor(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_tramitacao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_tramitacao'::regclass);

create index if not exists ix_legislativo_tramitacao_tenant_status on sigov.legislativo_tramitacao(tenant_id, status);

create index if not exists ix_legislativo_tramitacao_tenant_ativo on sigov.legislativo_tramitacao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_parecer (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_parecer'::regclass);

create index if not exists ix_legislativo_parecer_tenant_status on sigov.legislativo_parecer(tenant_id, status);

create index if not exists ix_legislativo_parecer_tenant_ativo on sigov.legislativo_parecer(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_sessao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_sessao'::regclass);

create index if not exists ix_legislativo_sessao_tenant_status on sigov.legislativo_sessao(tenant_id, status);

create index if not exists ix_legislativo_sessao_tenant_ativo on sigov.legislativo_sessao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_sessao_pauta (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_sessao_pauta'::regclass);

create index if not exists ix_legislativo_sessao_pauta_tenant_status on sigov.legislativo_sessao_pauta(tenant_id, status);

create index if not exists ix_legislativo_sessao_pauta_tenant_ativo on sigov.legislativo_sessao_pauta(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_sessao_item (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_sessao_item'::regclass);

create index if not exists ix_legislativo_sessao_item_tenant_status on sigov.legislativo_sessao_item(tenant_id, status);

create index if not exists ix_legislativo_sessao_item_tenant_ativo on sigov.legislativo_sessao_item(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_votacao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_votacao'::regclass);

create index if not exists ix_legislativo_votacao_tenant_status on sigov.legislativo_votacao(tenant_id, status);

create index if not exists ix_legislativo_votacao_tenant_ativo on sigov.legislativo_votacao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_voto (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_voto'::regclass);

create index if not exists ix_legislativo_voto_tenant_status on sigov.legislativo_voto(tenant_id, status);

create index if not exists ix_legislativo_voto_tenant_ativo on sigov.legislativo_voto(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_ata (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_ata'::regclass);

create index if not exists ix_legislativo_ata_tenant_status on sigov.legislativo_ata(tenant_id, status);

create index if not exists ix_legislativo_ata_tenant_ativo on sigov.legislativo_ata(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_norma (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_norma'::regclass);

create index if not exists ix_legislativo_norma_tenant_status on sigov.legislativo_norma(tenant_id, status);

create index if not exists ix_legislativo_norma_tenant_ativo on sigov.legislativo_norma(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_publicacao (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_publicacao'::regclass);

create index if not exists ix_legislativo_publicacao_tenant_status on sigov.legislativo_publicacao(tenant_id, status);

create index if not exists ix_legislativo_publicacao_tenant_ativo on sigov.legislativo_publicacao(tenant_id, is_deleted, ativo);

create table if not exists sigov.legislativo_evento (
    id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint not null, exercicio_id bigint,
    codigo varchar(100), numero bigint, ano integer, tipo varchar(80), status varchar(40) not null default 'ATIVO', titulo varchar(300), descricao text,
    referencia_id bigint, responsavel_id bigint, data_referencia timestamptz, prazo_at timestamptz, justificativa text, sigiloso boolean not null default false,
    hash_sha256 varchar(64), codigo_validacao varchar(100), dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb,
    correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false,
    created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint
);

select sigov.rc50_42_ensure_common_columns('sigov.legislativo_evento'::regclass);

create index if not exists ix_legislativo_evento_tenant_status on sigov.legislativo_evento(tenant_id, status);

create index if not exists ix_legislativo_evento_tenant_ativo on sigov.legislativo_evento(tenant_id, is_deleted, ativo);

drop function if exists sigov.rc50_42_ensure_common_columns(regclass);
