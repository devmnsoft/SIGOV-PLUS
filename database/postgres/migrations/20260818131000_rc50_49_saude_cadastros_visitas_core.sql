create schema if not exists sigov;
create table if not exists sigov.saude_domicilio (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_domicilio_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_domicilio_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_domicilio add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_domicilio_tenant_status on sigov.saude_domicilio(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_domicilio_moradia (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_domicilio_moradia_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_domicilio_moradia_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_domicilio_moradia add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_domicilio_moradia_tenant_status on sigov.saude_domicilio_moradia(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_domicilio_familia (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_domicilio_familia_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_domicilio_familia_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_domicilio_familia add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_domicilio_familia_tenant_status on sigov.saude_domicilio_familia(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_domicilio_membro (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_domicilio_membro_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_domicilio_membro_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_domicilio_membro add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_domicilio_membro_tenant_status on sigov.saude_domicilio_membro(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_cidadao_cadastro_individual (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_cidadao_cadastro_individual_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_cidadao_cadastro_individual_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_cidadao_cadastro_individual add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_cidadao_cadastro_individual_tenant_status on sigov.saude_cidadao_cadastro_individual(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_cidadao_condicao_saude (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_cidadao_condicao_saude_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_cidadao_condicao_saude_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_cidadao_condicao_saude add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_cidadao_condicao_saude_tenant_status on sigov.saude_cidadao_condicao_saude(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_visita_domiciliar (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_visita_domiciliar_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_visita_domiciliar_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_visita_domiciliar add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_visita_domiciliar_tenant_status on sigov.saude_visita_domiciliar(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_visita_motivo (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_visita_motivo_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_visita_motivo_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_visita_motivo add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_visita_motivo_tenant_status on sigov.saude_visita_motivo(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_visita_desfecho (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_visita_desfecho_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_visita_desfecho_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_visita_desfecho add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_visita_desfecho_tenant_status on sigov.saude_visita_desfecho(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_atividade_coletiva (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_atividade_coletiva_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_atividade_coletiva_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_atividade_coletiva add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_atividade_coletiva_tenant_status on sigov.saude_atividade_coletiva(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_atividade_coletiva_participante (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_atividade_coletiva_participante_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_atividade_coletiva_participante_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_atividade_coletiva_participante add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_atividade_coletiva_participante_tenant_status on sigov.saude_atividade_coletiva_participante(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_consumo_alimentar_marcador (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_consumo_alimentar_marcador_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_consumo_alimentar_marcador_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_consumo_alimentar_marcador add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_consumo_alimentar_marcador_tenant_status on sigov.saude_consumo_alimentar_marcador(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_ocorrencia_campo (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_ocorrencia_campo_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_ocorrencia_campo_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_ocorrencia_campo add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_ocorrencia_campo_tenant_status on sigov.saude_ocorrencia_campo(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_denuncia_dengue (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_denuncia_dengue_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_denuncia_dengue_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_denuncia_dengue add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_denuncia_dengue_tenant_status on sigov.saude_denuncia_dengue(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_acs_campo_evento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_acs_campo_evento_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_acs_campo_evento_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_acs_campo_evento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_acs_campo_evento_tenant_status on sigov.saude_acs_campo_evento(tenant_id,status) where is_deleted=false and ativo=true;
