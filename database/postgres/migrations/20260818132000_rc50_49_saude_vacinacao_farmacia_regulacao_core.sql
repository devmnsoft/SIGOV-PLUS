begin;
create schema if not exists sigov;
create table if not exists sigov.saude_vacina_calendario (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_vacina_calendario_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_vacina_calendario_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_vacina_calendario add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_vacina_calendario_tenant_status on sigov.saude_vacina_calendario(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_vacina_dose (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_vacina_dose_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_vacina_dose_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_vacina_dose add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_vacina_dose_tenant_status on sigov.saude_vacina_dose(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_vacinacao_evento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_vacinacao_evento_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_vacinacao_evento_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_vacinacao_evento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_vacinacao_evento_tenant_status on sigov.saude_vacinacao_evento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_farmacia_estoque (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_farmacia_estoque_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_farmacia_estoque_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_farmacia_estoque add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_farmacia_estoque_tenant_status on sigov.saude_farmacia_estoque(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_farmacia_dispensacao (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_farmacia_dispensacao_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_farmacia_dispensacao_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_farmacia_dispensacao add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_farmacia_dispensacao_tenant_status on sigov.saude_farmacia_dispensacao(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_farmacia_lote (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_farmacia_lote_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_farmacia_lote_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_farmacia_lote add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_farmacia_lote_tenant_status on sigov.saude_farmacia_lote(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_farmacia_alerta (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_farmacia_alerta_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_farmacia_alerta_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_farmacia_alerta add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_farmacia_alerta_tenant_status on sigov.saude_farmacia_alerta(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_regulacao_fila (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_regulacao_fila_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_regulacao_fila_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_regulacao_fila add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_regulacao_fila_tenant_status on sigov.saude_regulacao_fila(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_regulacao_prioridade (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_regulacao_prioridade_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_regulacao_prioridade_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_regulacao_prioridade add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_regulacao_prioridade_tenant_status on sigov.saude_regulacao_prioridade(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_regulacao_movimento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_regulacao_movimento_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_regulacao_movimento_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_regulacao_movimento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_regulacao_movimento_tenant_status on sigov.saude_regulacao_movimento(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_regulacao_documento_referencia (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_regulacao_documento_referencia_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_regulacao_documento_referencia_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_regulacao_documento_referencia add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_regulacao_documento_referencia_tenant_status on sigov.saude_regulacao_documento_referencia(tenant_id,status) where is_deleted=false and ativo=true;
create table if not exists sigov.saude_operacao_evento (id bigint generated by default as identity primary key, tenant_id bigint not null, entidade_id bigint, exercicio_id bigint, unidade_saude_id bigint, paciente_id bigint, profissional_id bigint, acs_id bigint, area_id bigint, microarea_id bigint, domicilio_id bigint, familia_id bigint, visita_id bigint, referencia_id bigint, lote_id bigint, dispositivo_id bigint, codigo varchar(120), numero bigint, chave_offline varchar(160), origem varchar(40) not null default 'WEB', tipo varchar(80), status varchar(50) not null default 'RASCUNHO', gravidade varchar(20), descricao text, justificativa text, latitude numeric(10,7), longitude numeric(10,7), quantidade numeric(18,4), saldo_minimo numeric(18,4), data_referencia timestamptz, validade date, dados jsonb not null default '{}'::jsonb, auditoria jsonb not null default '{}'::jsonb, correlation_id uuid not null default gen_random_uuid(), ativo boolean not null default true, is_deleted boolean not null default false, created_at timestamptz not null default now(), updated_at timestamptz, deleted_at timestamptz, created_by bigint, updated_by bigint, deleted_by bigint, constraint ck_saude_operacao_evento_latitude check(latitude is null or latitude between -90 and 90), constraint ck_saude_operacao_evento_longitude check(longitude is null or longitude between -180 and 180));
alter table sigov.saude_operacao_evento add column if not exists is_deleted boolean not null default false, add column if not exists ativo boolean not null default true, add column if not exists status varchar(50) not null default 'RASCUNHO', add column if not exists dados jsonb not null default '{}'::jsonb, add column if not exists auditoria jsonb not null default '{}'::jsonb, add column if not exists correlation_id uuid not null default gen_random_uuid(), add column if not exists created_at timestamptz not null default now(), add column if not exists created_by bigint, add column if not exists updated_by bigint, add column if not exists deleted_by bigint;
create index if not exists ix_saude_operacao_evento_tenant_status on sigov.saude_operacao_evento(tenant_id,status) where is_deleted=false and ativo=true;
commit;
