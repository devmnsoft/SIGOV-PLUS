# RC50.68E-R2 — Fechamento real dos gates da RC50.68

## Decisão executiva: **BLOCKED**

A R2 foi executada sobre `b425aad8fe0bdd9c0b0f1940005fe903fc785925`, merge do PR #270.
A RC50.68 **não foi promovida**: o ambiente fornecido não contém .NET SDK, PostgreSQL/`psql`,
PowerShell ou actionlint, não possui `SIGOV_CI_DB_PASSWORD` e o GitHub CLI não está autenticado.
Consequentemente, os P1 de build, banco e execução dos workflows continuam bloqueados. Nenhum
resultado foi convertido em PASS por inspeção ou inferência, e a RC50.69 não foi iniciada.

## Evidências dos gates obrigatórios

| Gate/comando | Resultado real |
|---|---|
| `dotnet --info` | **BLOCKED**, `dotnet: command not found`, exit 127 |
| `dotnet clean sigov.runtime.slnf` | **BLOCKED**, `dotnet: command not found`, exit 127 |
| `dotnet restore sigov.runtime.slnf --locked-mode` | **BLOCKED**, `dotnet: command not found`, exit 127 |
| `dotnet build sigov.runtime.slnf --configuration Release --no-restore --nologo -warnaserror` | **BLOCKED**, `dotnet: command not found`, exit 127 |
| `psql --version` e aplicação/reaplicação de `script_completop.sql` | **BLOCKED**, `psql: command not found`, exit 127; não há servidor PostgreSQL nem Docker |
| `pwsh -NoProfile -File scripts/generate-script-completop.ps1 -Verify` | **BLOCKED**, `pwsh: command not found`, exit 127 |
| `actionlint .github/workflows/*.yml` | **BLOCKED**, `actionlint: command not found`, exit 127 |
| Ruby `YAML.load_file(..., aliases: true)` nos oito workflows | **PASS**, exit 0 |
| `python3 scripts/validate-workflow-integrity.py .github/workflows/ci.yml` | **PASS**, `workflow integrity: PASS` |
| workflows dependentes do banco | **BLOCKED**, variável `SIGOV_CI_DB_PASSWORD` ausente e `gh auth status` informa sessão inexistente (exit 1) |

Foi tentada a instalação das ferramentas com `apt-get update`, mas o proxy do ambiente respondeu
HTTP 403 para os repositórios Ubuntu; tentativas HTTPS diretas também receberam `CONNECT tunnel
failed, response 403`. Isso não é evidência de aprovação dos gates: apenas identifica objetivamente
a limitação de infraestrutura.

## Banco e artefatos consolidados

Sem PostgreSQL 16+ não foi possível executar `ON_ERROR_STOP`, reaplicar o baseline, consultar
`sigov.schema_migrations`, provar as permissões de contexto/dashboard ou executar as consultas
Dapper do dashboard. Esses itens permanecem **BLOCKED**, e nenhuma migration corretiva foi criada
sem uma falha SQL real comprovada.

Como verificação estática auxiliar, as quatro saídas estruturais
(`script_completop.sql`, `script_completo.sql`, `database/script_completo.sql` e
`database/postgres/script_completo.sql`) são byte a byte iguais, com SHA-256
`064da132741f8ba3c4935b98f7c7e85deb72451ec46b84684d1c278c0d707202`. As duas saídas de
Development também são iguais entre si, com SHA-256
`a0079e4305d575bb06e2da79e78a2f18bab52c692c6be68afd1f97c6014d584f`. Isso não substitui o
`-Verify` em PowerShell nem a execução no servidor.

## Resultado e próximo passo

Não foi detectada falha de código ou SQL que pudesse ser corrigida neste ambiente. Para desbloquear,
um runner deve disponibilizar exatamente SDK 10.0.100, PostgreSQL 16+ e `psql`, PowerShell 7 e
actionlint; o secret deve estar configurado sem exposição; então todos os comandos e workflows
devem ser reexecutados. Só após todos os P1 passarem a decisão poderá mudar para **PROMOVIDA**.

