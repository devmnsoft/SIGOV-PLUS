# Idempotência Pós-RC 24

Status: failed

A aplicação dupla do manifest e de `script_completop.sql` depende de PostgreSQL 16 em execução. O CI deve substituir este status por evidência `equivalent` quando o serviço de banco estiver disponível.
