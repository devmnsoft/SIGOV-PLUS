# Schema report SIAFIC administrativo local

Relatório preparado para execução local via `scripts/schema-report-siafic-administrativo.ps1`. Neste ambiente, a conexão PostgreSQL real não foi informada, portanto nenhuma tabela administrativa foi ativada como persistência oficial.

Status: fallback honesto até execução contra banco homologado.
