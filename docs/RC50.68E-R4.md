# RC50.68E-R4 — Execução da esteira de promoção

## Decisão executiva: **BLOCKED**

Em 2026-08-20, a tentativa de executar a esteira partiu do commit candidato
`f6da64e3b756640b1322e7d0b8a3e506f7c92311`, atual `main` e merge do PR #272. O ambiente de
execução, porém, não possui autenticação para `github.com`: `gh auth status` informou que não há
login em nenhum host GitHub e não há `GH_TOKEN`/`GITHUB_TOKEN` disponível.

Por isso, não foi possível consultar os nomes dos repository secrets para confirmar a existência de
`SIGOV_CI_DB_PASSWORD`, confirmar pela API do Actions a disponibilidade do workflow, disparar o
`workflow_dispatch` ou acompanhar uma run. O valor do secret não foi solicitado nem impresso.

## Evidências reais coletadas

| Evidência | Resultado |
|---|---|
| candidato local | `git rev-parse HEAD` = `f6da64e3b756640b1322e7d0b8a3e506f7c92311` |
| autenticação GitHub | indisponível; `gh auth status` encerrou sem sessão autenticada |
| consulta do secret | **BLOCKED** antes da consulta; nenhum valor foi exposto |
| disponibilidade em Actions | **BLOCKED** antes da consulta autenticada |
| URL/commit/jobs/passos da run | inexistentes, pois nenhuma run foi disparada |
| falha comprovada no produto/SQL/workflow | nenhuma; a falha observada é exclusivamente de acesso ao GitHub |

Uma tentativa de consulta pública pela API também não substituiu a autenticação: a conexão foi
barrada pelo proxy do ambiente (`CONNECT tunnel failed`, HTTP 403). Essa limitação não autoriza
inferir a presença ou ausência do secret e tampouco autoriza declarar os gates como aprovados.

## Gates de promoção

Nenhum dos gates dinâmicos — build .NET 10, PostgreSQL 16, aplicação e reaplicação do baseline,
ledger `schema_migrations`, permissões SUPERADMIN, gerador PowerShell, actionlint e scans — foi
marcado como PASS nesta rodada. Não houve log de run que justificasse correção de YAML, código ou
SQL; portanto, nenhuma migration publicada foi alterada e nenhuma correção especulativa foi feita.

## Próxima ação obrigatória

Um operador com acesso ao repositório deve disponibilizar autenticação GitHub com permissões para
Actions e leitura dos nomes dos secrets, confirmar `SIGOV_CI_DB_PASSWORD` sem revelar seu valor e
disparar `RC50.68 promotion` no commit candidato vigente do `main`. A promoção exige a URL e o
resultado verde integral dessa execução.

A RC50.68 permanece **BLOCKED**. A RC50.69 ERP Serviços **não pode iniciar** e não foi iniciada.
