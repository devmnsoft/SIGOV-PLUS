# Runtime smoke tests

Ambiente de edição em 2026-07-02: `dotnet` e `docker` não estão instalados, portanto restore/build/Docker e smoke HTTP não puderam ser executados localmente. As rotas obrigatórias foram implementadas em controllers MVC/Razor para evitar 404 quando a aplicação estiver em ambiente com runtime.
