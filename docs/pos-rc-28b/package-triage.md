# Triagem de pacotes

A auditoria remota (`outdated`, `vulnerable` e `deprecated`) não foi executada porque o SDK .NET não existe no ambiente. `Directory.Packages.props` precisa ser validado contra o NuGet antes de qualquer escolha de versão; nenhuma versão foi alterada por presunção.
