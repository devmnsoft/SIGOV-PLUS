# Roadmap próxima sprint

1. Criar migrations não destrutivas para `assinatura_saas`, `notificacao`, `tenant_uso_mensal` e suporte/faturas se ainda não existirem.
2. Completar POST real de planos/assinaturas com validação coluna-a-coluna e modais de confirmação.
3. Evoluir white label persistente e aplicação dinâmica de cores/logo/favicons por tenant.
4. Persistir onboarding em `tenant_implantacao` ou `tenant.metadados`.
5. Consolidar `UserPermissionService` e `MenuAuthorizationService` com matriz real de permissões.
