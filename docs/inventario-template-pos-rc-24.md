# Inventário do template Pós-RC 24

- CSS principal e arquivos SIGOV em `src/Sigov.Web/wwwroot/css`.
- Layout compartilhado em `src/Sigov.Web/Views/Shared`.
- Controladores web em `src/Sigov.Web/Controllers`.
- Contratos operacionais em `src/Sigov.Application/Operational`.
- Migrations PostgreSQL em `database/postgres/migrations`.
- Scripts de release e validação em `scripts`.
