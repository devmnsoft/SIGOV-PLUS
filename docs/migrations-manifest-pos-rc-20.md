# Manifest de migrations Pós-RC 20

Foi criado `database/postgres/migrations/manifest.json` com categoria, inclusão em baseline, aplicação automática e checksum SHA-256 determinístico por migration. Seeds de desenvolvimento ficam excluídos da baseline estrutural.
