# Evidências Pós-RC 20

Evidências locais disponíveis nesta execução:

- Inspeção estática do repositório.
- `dotnet build` não pôde ser executado localmente porque `dotnet` não está instalado na imagem.
- `pwsh` não pôde ser executado localmente porque PowerShell não está instalado na imagem.
- PostgreSQL não pôde ser executado localmente porque cliente/servidor não estão instalados na imagem.

A validação final deve ser confirmada pelo GitHub Actions após abertura do PR draft.
