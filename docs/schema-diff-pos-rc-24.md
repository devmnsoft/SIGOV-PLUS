# Schema diff Pós-RC 24

Status: failed

A comparação integral entre `sigov_migrations` e `sigov_baseline` requer PostgreSQL 16 disponível. No ambiente local desta execução não há `psql`/PostgreSQL provisionado, portanto o documento registra a limitação e mantém o status permitido `failed` até execução no CI.
