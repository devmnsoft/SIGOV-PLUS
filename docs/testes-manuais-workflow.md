# Testes manuais workflow

Checklist: abrir `/Workflow`, `/Tarefas`, `/Notificacoes`, `/Agenda`, `/Integracoes`, `/Bi`, `/MobileCampo`, `/Busca?q=teste`, `/Relatorios`, `/Dashboard`, `/MinhaCentral`; conferir fallback honesto sem schema; confirmar que POSTs usam antiforgery; validar auditoria em ações críticas; verificar ausência de stacktrace para usuário final.
