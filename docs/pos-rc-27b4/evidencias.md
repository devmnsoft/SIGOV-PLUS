# Evidências

| Verificação | Resultado local |
|---|---|
| SHA base | confirmado |
| Busca `WebApplicationFactory<Program>` em API/Integration | zero após correção |
| `git diff --check` | aprovado |
| Build/testes | não executados: `dotnet` ausente |
| Baseline | não executado: `pwsh` ausente |
| Workflow remoto/TRX | não consultado: `gh` ausente |

Este documento registra limitações reais; não declara gates verdes sem execução.
