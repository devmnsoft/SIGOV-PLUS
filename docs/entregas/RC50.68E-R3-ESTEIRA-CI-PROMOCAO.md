# Entrega RC50.68E-R3 — Esteira CI de promoção

## Resultado entregue

- workflow exclusivamente manual, com permissões mínimas e preflight explícito do secret;
- runner com SDK .NET 10.0.100 e PostgreSQL 16 isolado por service container;
- toolchain e todos os comandos de aceite da RC50.68 executados em modo fail-fast;
- aplicação dupla do baseline, consulta do ledger e asserções SQL das RC50.68A–D;
- gates de autoridade persistida SUPERADMIN, drift dos consolidados e scans preventivos.

## Matriz de aceite desta entrega

| Critério | Estado nesta alteração | Evidência esperada para promoção |
|---|---|---|
| esteira manual reproduzível | ENTREGUE | workflow `RC50.68 promotion` disponível em Actions |
| secret protegido | ENTREGUE | preflight registra apenas que está configurado ou `BLOCKED` |
| build .NET 10 real | AGUARDA RUN | log verde do job de promoção |
| baseline PostgreSQL aplicado duas vezes | AGUARDA RUN | duas invocações `psql` com exit 0 |
| migrations e permissões A–D | AGUARDA RUN | bloco de asserções SQL com exit 0 |
| promoção RC50.68 | **BLOCKED** | todos os passos da mesma run devem ficar verdes |

Não se declara PASS com base na revisão do YAML. Uma falha da run será tratada como evidência real,
sem simulação de sucesso. A RC50.69 permanece fora do escopo e não foi iniciada.
