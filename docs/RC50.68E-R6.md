# RC50.68E-R6 — decisão técnica final da promoção

## Decisão: **BLOCKED**

Em 2026-08-24 foi executada localmente a esteira Bash sobre o SHA
`79fd51224b9bb0a0513b88d240f63b23885626d5`. Não havia evidência anterior em
`artifacts/rc50-68-local-promotion/`; a execução R6 produziu `result.json`, `summary.md` e
`promotion.log` localmente, todos ignorados pelo Git. O processo retornou **2**, sem FAIL.

Passaram com execução real: arquivos obrigatórios, Node.js `v20.20.2`, `git diff --check`, parse e
SHA-256 do manifest, `node --check`, `bash -n` e parse dos workflows por Ruby/Psych. Ficaram
**BLOCKED**: .NET, `psql`, conexão PostgreSQL segura, aplicação/reaplicação do baseline, asserções
persistentes, ledger e smoke. PowerShell também não estava instalado e não recebeu PASS inferido.

O bloqueio é exclusivamente de capacidade deste ambiente, não um FAIL conhecido do repositório.
Sem .NET 10, PostgreSQL 16/`psql`, banco descartável confirmado e credencial local para o smoke
autenticado, não há base para declarar a RC50.68 promovível. O CI oficial não foi consultado nem
executado e permanece um gate distinto. A RC50.69 continua não iniciada.

## Correções R6 na esteira

- evidência Bash finalizada por `trap`, inclusive em saída antecipada, com JSON mínimo válido e
  `BLOCKED` se o escritor Python não existir;
- decisão local calculada: somente uma matriz integralmente PASS pode emitir
  `PROMOVÍVEL LOCALMENTE`; FAIL retorna 1 e BLOCKED retorna 2;
- sanitização ampliada para Cookie, Set-Cookie e Bearer, e recusa conservadora de host contendo
  marcador `prod`/`production` em Bash e PowerShell;
- metadados R6 e separação explícita entre decisão local e CI oficial.

O diagnóstico reproduzível e os próximos passos estão em
[`execucao/RC50.68-DECISAO-TECNICA-FINAL.md`](execucao/RC50.68-DECISAO-TECNICA-FINAL.md).
