# Matriz de rotas

| Área | Contrato pretendido | Estado nesta etapa |
|---|---|---|
| Health live | `/api/health/live`, independente de banco | a validar em CI |
| Tarefas Web | `/Tarefas` e jornadas específicas | bloqueado pelo Portão B |
| Tarefas API | `/api/operacional/tarefas` | bloqueado pelo Portão B |
| Kanban Tarefas | `/Kanban/Tarefas/Mover` com IDs `long` | bloqueado pelo Portão B |
