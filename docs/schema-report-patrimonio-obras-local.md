# Schema report — Patrimônio, Inventário e Obras

Status: Em implantação neste ambiente.

A validação inicial não conseguiu executar consulta real porque o runtime local não possui `dotnet`, `docker` ou instância PostgreSQL SIGOV ativa. Use `scripts/schema-report-patrimonio-obras.ps1` com `SIGOV_DATABASE_URL` para gerar a saída real antes de ativar persistência.
