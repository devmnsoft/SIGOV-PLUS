@* Partial RH incremental: campos tipados são renderizados pelo rh.js a partir do data-recurso, mantendo compatibilidade com o CRUD genérico JSONB. *@
<div class="alert alert-light border small mb-0">
  <strong>RH tipado incremental.</strong> Formulário com antiforgery, Bootstrap 5, jQuery/Ajax, validação no backend, tenant isolation, auditoria e mascaramento LGPD por padrão.
</div>
