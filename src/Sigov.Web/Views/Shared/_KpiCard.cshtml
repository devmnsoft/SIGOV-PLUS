@model dynamic
<article class="sigov-kpi-card"><div class="sigov-kpi-card__label">@Model?.Title</div><div class="sigov-kpi-card__value">@Model?.Value</div><p class="text-muted mb-0">@Model?.Description</p></article>
