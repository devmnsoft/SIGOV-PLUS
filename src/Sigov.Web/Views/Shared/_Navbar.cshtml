<nav class="sigov-topbar" aria-label="Barra superior SIGOV">
  <div class="sigov-topbar__left">
    <button class="sigov-icon-btn d-lg-none" type="button" data-sigov-sidebar-toggle aria-controls="sigovSidebar" aria-expanded="false" aria-label="Abrir menu">☰</button>
    <a class="sigov-brand" href="/Dashboard" aria-label="Ir para o dashboard">
      <img class="sigov-logo" src="~/img/sigov-logo.svg" alt="SIGOV PLUS" />
      <span class="sigov-brand__text">SIGOV <strong>PLUS</strong></span>
    </a>
  </div>
  <div class="sigov-topbar__context" aria-label="Contexto da sessão">
    <span class="sigov-context-pill">Tenant: <strong>Principal</strong></span>
    <span class="sigov-context-pill">Entidade: <strong>Matriz</strong></span>
    <span class="sigov-context-pill">Exercício: <strong>@DateTime.UtcNow.Year</strong></span>
    <span class="sigov-badge sigov-badge--success">Ambiente seguro</span>
    <span class="sigov-badge sigov-badge--info">@(System.Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") ?? "Production")</span>
  </div>
  <div class="sigov-topbar__actions">
    <a class="sigov-btn sigov-btn--ghost sigov-btn--sm" href="/Operacao/Health">Health</a>
    <button class="sigov-btn sigov-btn--ghost sigov-btn--sm" type="button" data-sigov-theme-toggle>Tema</button>
    <a class="sigov-btn sigov-btn--ghost sigov-btn--sm" href="/Ajuda">Ajuda</a>
    <span class="sigov-user-chip">Admin</span>
    <a class="sigov-btn sigov-btn--primary sigov-btn--sm" href="/Auth/Logout">Sair</a>
  </div>
</nav>
