(function (window, $) { class SigovModal { open(selector) { var el = document.querySelector(selector); if (el && window.bootstrap) { new window.bootstrap.Modal(el).show(); } } } class SigovConfirmModal { confirm(message) { return window.confirm(message || 'Confirma a operação?'); } } window.Sigov = window.Sigov || {}; window.Sigov.modal = new SigovModal(); window.Sigov.confirmModal = new SigovConfirmModal(); window.Sigov.SigovModal = SigovModal; window.Sigov.SigovConfirmModal = SigovConfirmModal; $('[data-sigov-confirm]').on('click', function (e) { if (!window.Sigov.confirmModal.confirm($(this).data('sigov-confirm'))) { e.preventDefault(); } }); })(window, window.jQuery);
