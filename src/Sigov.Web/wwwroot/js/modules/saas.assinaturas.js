(function ($) { 'use strict'; $(function () { const a = $('#minhaAssinatura'); if (a.length) $.getJSON('/api/tenant/minha-assinatura').done(r => a.html(`<h2>${r.data.planoNome}</h2><p>Status: ${r.data.status}</p><p>Usuários contratados: ${r.data.usuariosContratados}</p><p>Módulos: ${(r.data.modulos || []).join(', ')}</p>`)); const m = $('#meusModulos'); if (m.length) $.getJSON('/api/tenant/meus-modulos').done(r => m.html((r.data || []).map(x => `<div class="col-md-3"><div class="card card-body">${x}</div></div>`).join(''))); }); })(jQuery);
