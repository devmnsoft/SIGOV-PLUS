$(function () {
  const api = window.sigovApi;
  const ui = window.sigovUi;
  function escapeHtml(v) { return $('<div>').text(v == null ? '' : String(v)).html(); }
  function badge(v) { return `<span class="badge bg-secondary">${escapeHtml(v)}</span>`; }
  function formObject($form) { const o = {}; $form.serializeArray().forEach(x => { o[x.name] = x.value; }); return o; }
  function handleError(e) { ui.notify(e.message && e.message.includes('403') ? 'Acesso negado.' : (e.message || 'Erro inesperado.'), 'danger'); }
  async function carregar() {
    const data = await api.request('/api/processos?' + $('#processos-filtro').serialize());
    const rows = (data.data.items || []).map(p => `<tr><td>${escapeHtml(p.numero)}</td><td>${escapeHtml(p.assunto)}</td><td>${escapeHtml(p.tipoProcesso)}</td><td>${escapeHtml(p.interessado || '-')}</td><td>${badge(p.status)}</td><td>${badge(p.prioridade)}</td><td>${new Date(p.dataAbertura).toLocaleDateString()}</td><td>${p.prazoRespostaAt ? new Date(p.prazoRespostaAt).toLocaleDateString() : 'Prazo não configurado'}</td><td><a class="btn btn-sm btn-outline-primary" href="/Processos/Detalhe/${p.id}">Ver</a> <button class="btn btn-sm btn-outline-secondary js-mov" data-id="${p.id}" data-status="${escapeHtml(p.status)}">Encaminhar</button> <button class="btn btn-sm btn-outline-info js-par" data-id="${p.id}">Parecer</button></td></tr>`).join('');
    $('#processos-table tbody').html(rows || '<tr><td colspan="9" class="text-muted">Nenhum processo encontrado.</td></tr>');
  }
  $('#processos-filtro').on('submit', function (e) { e.preventDefault(); carregar().catch(handleError); });
  $('#processo-form').on('submit', async function (e) { e.preventDefault(); if (!$(this).valid()) return; const f = formObject($(this)); const body = { tipoProcessoId: Number(f.TipoProcessoId), assunto: f.Assunto, descricao: f.Descricao, interessadoPessoaId: f.InteressadoPessoaId ? Number(f.InteressadoPessoaId) : null, unidadeOrigemId: f.UnidadeOrigemId ? Number(f.UnidadeOrigemId) : null, unidadeAtualId: f.UnidadeAtualId ? Number(f.UnidadeAtualId) : null, prioridade: f.Prioridade, sigiloso: !!f.Sigiloso, prazoRespostaAt: f.PrazoRespostaAt || null, observacao: f.Observacao }; try { await api.request('/api/processos', { method: 'POST', body: JSON.stringify(body) }); ui.notify('Processo salvo com sucesso.', 'success'); } catch (e) { handleError(e); } });
  $(document).on('click', '.js-mov', function () { $('#ProcessoId').val($(this).data('id')); $('#StatusEsperado').val($(this).data('status')); new bootstrap.Modal('#modalMovimentar').show(); });
  $(document).on('click', '.js-par', function () { $('#ProcessoId').val($(this).data('id')); new bootstrap.Modal('#modalParecer').show(); });
  $('#movimentar-form').on('submit', async function (e) { e.preventDefault(); const f = formObject($(this)); const $button = $(this).find(':submit').prop('disabled', true); try { await api.request(`/api/processos/${f.ProcessoId}/movimentar`, { method: 'POST', body: JSON.stringify({ unidadeDestinoId: f.UnidadeDestinoId ? Number(f.UnidadeDestinoId) : null, usuarioDestinoId: f.UsuarioDestinoId ? Number(f.UsuarioDestinoId) : null, despacho: f.Despacho, statusNovo: f.StatusNovo || null, statusEsperado: f.StatusEsperado || null }) }); ui.notify('Encaminhamento registrado no histórico.', 'success'); window.location.reload(); } catch (e) { handleError(e); $button.prop('disabled', false); } });
  $('#parecer-form').on('submit', async function (e) { e.preventDefault(); const f = formObject($(this)); try { await api.request(`/api/processos/${f.ProcessoId}/pareceres`, { method: 'POST', body: JSON.stringify({ titulo: f.Titulo, texto: f.Texto, tipoParecer: f.TipoParecer || 'TECNICO', sigiloso: !!f.Sigiloso }) }); ui.notify('Parecer emitido.', 'success'); } catch (e) { handleError(e); } });
  const detalheId = $('#processo-id').val();
  if (detalheId) api.request(`/api/processos/${detalheId}`).then(r => { const p = r.data; const finalizado = p.status === 'ENCERRADO' || p.status === 'CANCELADO'; $('#processo-numero').text(p.numero); $('#StatusEsperado').val(p.status); $('#dados').html(`<h2>${escapeHtml(p.assunto)}</h2><p>${escapeHtml(p.descricao || 'Sem descrição.')}</p><p>Status: ${badge(p.status)} Prioridade: ${badge(p.prioridade)}</p><p><strong>Prazo:</strong> ${p.prazoRespostaAt ? new Date(p.prazoRespostaAt).toLocaleString() : 'Prazo não configurado'}</p>${finalizado ? '<p class="alert alert-secondary">Processo finalizado: alterações operacionais estão bloqueadas.</p>' : `<div class="d-flex gap-2 flex-wrap"><button class="btn btn-primary js-mov" data-id="${p.id}" data-status="${escapeHtml(p.status)}">Encaminhar</button></div><form id="finalizar-processo" class="mt-3"><label class="form-label" for="justificativa-final">Justificativa para conclusão ou cancelamento</label><textarea id="justificativa-final" class="form-control" required></textarea><div class="mt-2 d-flex gap-2"><button class="btn btn-success" data-acao="encerrar" type="submit">Concluir processo</button><button class="btn btn-outline-danger" data-acao="cancelar" type="submit">Cancelar processo</button></div></form>`}`); $('#processo-timeline').html((p.movimentacoes || []).map(m => `<div class="list-group-item"><strong>${new Date(m.movimentadoAt).toLocaleString()}</strong><br>${escapeHtml(m.despacho)}<br><small>${escapeHtml(m.statusAnterior || '')} → ${escapeHtml(m.statusNovo || '')}</small></div>`).join('') || '<div class="list-group-item text-muted">Sem movimentações.</div>'); $('#finalizar-processo').on('submit', async function (e) { e.preventDefault(); const acao = $(e.originalEvent && e.originalEvent.submitter).data('acao'); if (!acao) return; const justificativa = $('#justificativa-final').val(); const $buttons = $(this).find('button').prop('disabled', true); try { await api.request(`/api/processos/${p.id}/${acao}`, { method: 'POST', body: JSON.stringify({ justificativa, statusEsperado: p.status }) }); ui.notify(acao === 'encerrar' ? 'Processo concluído e histórico registrado.' : 'Processo cancelado e histórico registrado.', 'success'); window.location.reload(); } catch (e) { handleError(e); $buttons.prop('disabled', false); } }); }).catch(handleError);
});
