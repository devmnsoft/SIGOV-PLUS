(function ($) { 'use strict'; function load(){ var $g=$('#agroIndicadoresGrid'); if(!$g.length)return; $.getJSON('/api/agro/bi/indicadores').done(function(r){var data=(r.data||r)||[]; $g.html('<div class="list-group">'+data.map(function(x){return '<div class="list-group-item d-flex justify-content-between"><span><strong>'+x.codigo+'</strong> - '+x.nome+'</span><span class="badge bg-secondary">'+x.categoria+'</span></div>';}).join('')+'</div>');}).fail(function(xhr){$g.html('<div class="alert alert-danger">Erro '+xhr.status+'</div>');});} $(function(){load(); $('#formAgroIndicador').on('submit',function(e){e.preventDefault(); var payload={codigo:this.codigo.value,nome:this.nome.value,categoria:this.categoria.value,publico:false}; $.ajax({url:'/api/agro/bi/indicadores',method:'POST',contentType:'application/json',data:JSON.stringify(payload)}).done(load).fail(function(xhr){$('#agroIndicadoresToast').removeClass('d-none alert-success').addClass('alert-danger').text('Falha ao salvar: '+xhr.status);});});});})(jQuery);
