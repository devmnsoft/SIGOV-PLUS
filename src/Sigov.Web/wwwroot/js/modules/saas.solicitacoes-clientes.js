(function ($) { 'use strict'; $(function () { const el = $('#saasSolicitacoes'); if (el.length) $.getJSON('/api/saas/solicitacoes-clientes').done(r => el.html(`<table class="table"><tbody>${(r.data || []).map(s => `<tr><td>${s.protocolo}</td><td>${s.nomeOrganizacao}</td><td>${s.emailResponsavelMascarado}</td><td>${s.status}</td></tr>`).join('')}</tbody></table>`)); }); })(jQuery);
