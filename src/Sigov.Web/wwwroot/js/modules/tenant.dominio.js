(function ($) { 'use strict'; function load(){ const el=$('#tenantDominios'); if(el.length) $.getJSON('/api/tenant/dominios').done(r=>el.html((r.data||[]).map(d=>`<div class="list-group-item d-flex justify-content-between"><span>${d.dominio}</span><span class="badge bg-secondary">${d.status}</span></div>`).join(''))); } $(function(){ load(); $('#formTenantDominio').on('submit', function(e){ e.preventDefault(); $.ajax({url:'/api/tenant/dominios',method:'POST',contentType:'application/json',data:JSON.stringify({dominio: $('[name=dominio]').val()})}).done(load).fail(()=>alert('Plano não permite domínio customizado ou domínio inválido.')); }); }); })(jQuery);
