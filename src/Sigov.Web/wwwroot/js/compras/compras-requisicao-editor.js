import { request, lock } from './compras-api.js';
const form=document.querySelector('#requisicao-form'), host=form?.querySelector('[data-items]'), template=document.querySelector('#item-template');
function add(){const row=template.content.firstElementChild.cloneNode(true);row.querySelector('[data-remove]').addEventListener('click',()=>row.remove());host.append(row);}
form?.querySelector('[data-add-item]')?.addEventListener('click',add); if(form) add();
form?.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]'),feedback=form.querySelector('.form-feedback');lock(button,true);const values=Object.fromEntries(new FormData(form));const items=[...host.querySelectorAll('.item-row')].map(row=>({tipo:row.querySelector('[data-field="tipo"]').value,descricao:row.querySelector('[data-field="descricao"]').value,especificacao:null,unidade:row.querySelector('[data-field="unidade"]').value,quantidade:Number(row.querySelector('[data-field="quantidade"]').value),valorEstimado:Number(row.querySelector('[data-field="valorEstimado"]').value),permiteParcial:true,exigeInspecao:false}));try{const result=await request('/api/compras-empresariais/requisicoes',{method:'POST',body:JSON.stringify({setor:values.setor,urgencia:values.urgencia,justificativa:values.justificativa,observacoes:null,dataNecessaria:null,centroCustoId:null,projetoId:null,contratoId:null,ordemServicoId:null,almoxarifadoId:null,itens:items})});window.location.assign(`/ComprasEmpresariais/Requisicoes/${result.id}`);}catch(error){feedback.textContent=error.message;feedback.classList.add('is-error');}finally{lock(button,false);}});
