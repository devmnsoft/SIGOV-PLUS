import {api,key,toast} from './api.js'; const root=document.querySelector('[data-os-list]'),results=root.querySelector('[data-results]');let controller;async function load(){controller?.abort();controller=new AbortController();const p=new URLSearchParams(new FormData(root.querySelector('[data-filter]')));try{const data=await api(`/api/ordens-servico?${p}`);results.innerHTML=data.items?.length?data.items.map(x=>`<article class="card"><div class="card-body"><div class="d-flex justify-content-between"><strong>${x.numero}</strong><span class="badge text-bg-primary">${x.status}</span></div><h2 class="h5 mt-2">${x.cliente}</h2><a class="stretched-link" href="/OrdemServico/Ordens/${x.id}">Abrir atendimento</a></div></article>`).join(''):'<div class="text-center p-5"><h2 class="h5">Nenhuma ordem encontrada</h2><p>Ajuste os filtros ou crie uma ordem.</p></div>';}catch(e){toast(e.message,'danger')}}let timer;root.querySelector('[data-filter]').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(load,300)});root.querySelector('[data-create]').addEventListener('submit',async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;try{const f=Object.fromEntries(new FormData(e.currentTarget));await api('/api/ordens-servico',{method:'POST',headers:{'Idempotency-Key':key()},body:JSON.stringify(f)});bootstrap.Modal.getInstance(document.querySelector('#novaOs')).hide();e.currentTarget.reset();toast('Ordem criada.');await load();}catch(err){toast(err.message,'danger')}finally{b.disabled=false}});load();
