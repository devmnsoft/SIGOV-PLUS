export async function api(url, options={}) { const controller=new AbortController(); const timeout=setTimeout(()=>controller.abort(),20000); try { const response=await fetch(url,{...options,signal:controller.signal,headers:{Accept:'application/json','Content-Type':'application/json',...options.headers}}); if(!response.ok){const problem=await response.json().catch(()=>({}));throw new Error(problem.detail||problem.message||`Operação recusada (${response.status})`);} return response.status===204?null:response.json(); } finally { clearTimeout(timeout); } }
export const key=()=>crypto.randomUUID();
export function toast(message,type='success'){const el=document.createElement('div');el.className=`toast show text-bg-${type} position-fixed bottom-0 end-0 m-3`;el.setAttribute('role','status');el.innerHTML=`<div class="d-flex"><div class="toast-body"></div><button class="btn-close btn-close-white me-2 m-auto" aria-label="Fechar"></button></div>`;el.querySelector('.toast-body').textContent=message;el.querySelector('button').onclick=()=>el.remove();document.body.append(el);setTimeout(()=>el.remove(),5000);}
